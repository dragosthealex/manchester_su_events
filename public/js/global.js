$(document).ready(function() {
  // Init stuff
  var init = function() {
    // $("html").niceScroll();
  }

  init();

  // Bootstrap notify defaults
  $.notifyDefaults({
    placement:{from: 'bottom',align: 'center'},
  });
});
